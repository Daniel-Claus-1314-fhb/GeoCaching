<Page
    x:Class="GeoCacheingFinder.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GeoCacheingFinder"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" HorizontalAlignment="Left" TextWrapping="Wrap" Text="GeoCacheTools" VerticalAlignment="Top" Style="{StaticResource HeaderTextBlockStyle}"/>
    
        <ListView Grid.Row="1" HorizontalAlignment="Center">
            <ListViewItem>
                <Button Content="Geo Cache List" Click="Button_Click"/>
            </ListViewItem>
        </ListView>
    </Grid>
</Page>